import React from "react";
import Homepage from "./homepage/Homepage";

interface pageProps {}

export default function page({}: pageProps) {
  return (
    <div>
      <Homepage />
    </div>
  );
}
